import React from 'react'
  import * as Styles from './CovidLink.styles'

  type PropsType = {}
  const CovidLink = ({}:PropsType) => {
  return <Styles.CovidLinkWrapper></Styles.CovidLinkWrapper>
}

export default CovidLink
