import React from 'react'
  import * as Styles from './Social.styles'

  type PropsType = {}
  const Social = ({}:PropsType) => {
  return <Styles.SocialWrapper></Styles.SocialWrapper>
}

export default Social
