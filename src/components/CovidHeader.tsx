import React from 'react'
  import * as Styles from './CovidHeader.styles'

  type PropsType = {}
  const CovidHeader = ({}:PropsType) => {
  return <Styles.CovidHeaderWrapper></Styles.CovidHeaderWrapper>
}

export default CovidHeader
